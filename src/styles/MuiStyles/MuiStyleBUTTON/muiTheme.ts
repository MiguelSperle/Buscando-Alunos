import { createTheme } from '@mui/material/styles'; 

export const themeButton = createTheme({ 
    palette: {
        primary: {
            main: '#fff',
            contrastText: '#D3D3D3'
        }
    }
})